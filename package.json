{
  "name": "cherry-design",
  "description": "cherry Design Vue 2.0 Monorepo",
  "version": "0.2.1",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "yarn workspace @cherry-design/vue-ui run start",
    "build": "yarn workspace @cherry-design/vue-ui run build",
    "storybook": "yarn workspace @cherry-design/vue-ui-storybook run storybook",
    "dev": "yarn workspace @cherry-design/vue-ui-storybook run storybook",
    "site": "yarn workspace @cherry-design/vue-ui-storybook run build-storybook",
    "pub": "yarn build && lerna publish"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=14.13.1"
  },
  "author": "alanuuuu"
}
